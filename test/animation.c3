
import ennoia;

/*
fn void creation() @test {
  ennoia::init();
  defer ennoia::clean();

  // Texture
  AnimatedTexture texAni = {
    
  };

  while (!ennoia::window_should_close()) {
    texAni.draw({0,0})!!;

    ennoia::drawing();
  }
}
*/

fn void texture() @test {}


